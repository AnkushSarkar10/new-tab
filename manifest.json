{
  "manifest_version": 2,
  "name": "new tab",
  "version": "0.1.1",
  "description": "replace browsers new tab page with a random fullscreen image from a selected album, or have a website as the new tab.",
  "permissions": ["storage"],
  "chrome_url_overrides": {
    "newtab": "newtab.html"
  },
  "browser_specific_settings": {
    "gecko": {
      "id": "album-new-tab@local",
      "data_collection_permissions": {
        "required": ["none"]
      }
    }
  }
}
